import React from 'react';
import { ListContainer } from './containers';

function App() {
  return (
    <div className="container">
      <ListContainer />
    </div>
  );
}

export default App;

// results 30 pre page
// total_count === results

// 2 / 30 > 1
